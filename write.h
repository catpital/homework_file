#pragma once


//void reading_user_file(std::string namefile, User _obj) {
	/*int i = 0;

	while (!(namefile.c_str()).eof() && (i < v.size())) {
		namefile.c_str()) >> obj;
		v[i] = obj;
		cout << v[i] << endl;
		i++;
	}*/
                         }